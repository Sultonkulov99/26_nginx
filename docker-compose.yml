version: '3.9'

services:
  app:
    build: .
    container_name: 26_app
    restart: always
    env_file:
      - .env
    ports:
      - '4000:3000'
    command: >
      sh -c "
        npm run start:prod
      "
